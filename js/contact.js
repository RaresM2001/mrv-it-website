/**
 * JS SCRIPT FOR CONTACT PAGE
 * 
 * This script handles the contact div animation as well
 * as the functionality that actually sends the email using 
 * sendgrid provider.
 * 
 * @author Modure Rares
 */

$(document).ready(function() {

    // this triggers whenever a contact button is pressed
    $('.contact-btn').on('click', function() {
        alert('Contact btn pressed!');
    });
});